import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  subHeader: {
    paddingHorizontal: 20,
    paddingTop: 20,
    paddingBottom: 10,
    fontWeight: '500',
    fontSize: 18,
  },
});
